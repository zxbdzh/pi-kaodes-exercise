# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.3.0] - 2026-09-19

### 新增

- **AI 追问面板（F）**：主区域内多轮对话，不再是一次性弹窗；对话历史挂在当前题并随断点持久化，可连续追问，`T` 点拨结论自动进入历史可接着问。回答带考点高亮（仅 LLM 认定词，首现、封顶 4 处），等待时显示「… 思考中」，失败时状态栏给出具体原因并撤回未回应提问。
- **闪卡独立模块**：
  - 答错后按 `Y`，AI 结合本题累计错误次数与本组相似错题自动组卡入集；同一题重复入卡累加错误次数。闪卡集持久化到 `~/.pi/kaodes-flashcards.json`，跨章节、跨会话保留。
  - 按 `K`（或 `:card`）抽卡复习：错得多优先、久未复习优先、新卡最优先（加权排序），`enter` 翻面看要点，再 `enter` 记一次复习并下一张，`esc` 退出。
- **错题集中回顾（W / `:wrong`）**：列出本组全部答错的题（你答 vs 正确答案），`↑↓` 选择、`enter` 直跳该题。

### 变更

- **防剧透**：未作答前选项不再标考点词（哪个词被标色本身就是答案线索），作答或展开解析后恢复为复习标注；题干保留否定设问红标等导航语义。
- **追问即追问**：AI 回答不再附带记忆卡，移除旧「回答附记忆卡 + C 翻看」路径。
- 闪卡 / 对话 / 错题 / 退出框等模态视图与题目视图共用同一套框与换行渲染主路径，切换时强制宿主全量重绘。

### 修复

- 修复**追问必失败**：assistant 历史消息的 content 需为 block 数组（宿主 `transformMessages` 对字符串直接崩溃），此前仅无历史的首问可用。
- 修复模态视图切换时宿主行级增量渲染残留旧帧导致的**界面错位**（同一行重复出现等）。
- AI 调用失败不再静默吞错：错误透出到状态栏，便于定位模型通道问题。
- 对话视图保留 AI 回复原有换行结构，显示层剥离 markdown 星号，不再把多行回答挤成一坨。

## [1.2.0] - 2026-09-19

### 新增

- **LLM 语义高亮（第二阶段）**：按 `T` 点拨或 `F` 问 AI 时，AI 在回答末尾顺带返回本题考点词（`【高亮】` 块），插件将其映射到题干/选项并与规则高亮合并渲染。**搭车已有的 AI 调用，不产生额外请求**；结果缓存进该题断点，离线可复现，重叠处以 LLM 判定为准。
- 高亮管线新增 `marksFromTerms` / `mergeMarks` / `parseHighlightBlock`：只信任词面（`indexOf` 定位），不采用模型返回的数字偏移，保证截断换行后仍不错位。
- AI 不可用时自动回落到规则词典高亮，`:hl off` 同时关闭两层高亮。

## [1.1.0] - 2026-09-19

### 新增

- **规则高亮**：题干题眼词（根本/本质/关键/前提…）标黄、否定设问（错误的是/不属于…）标红、解析结论词（因此/由此可见…）标绿、选项公共前缀弱化，只保留差异部分，帮助快速抓考点。
- **`:hl [on|off]`** 答题页指令，随时开关规则高亮。
- **分页选择器**：科目 / 课程 / 章节列表支持 `←→` 翻页、页码提示，长列表不再一屏塞不下。
- **多级返回**：选择列表按 `⌫ Backspace` 逐级返回上一层（科目页再按则退出）。
- **`/kaodes version`**：显示插件版本号与当前加载的构建目录，方便确认更新是否生效。
- **`/kaodes stats`**：学习统计看板，汇总本地断点的覆盖章节、题量、正确/错误数、正确率进度条，并按科目聚合。
- **`/kaodes prefs`**：查看当前偏好（规则高亮开关、分页每页条数）。
- **偏好持久化**：高亮开关与分页大小写入 `~/.pi/kaodes-prefs.json`，答题页 `:hl` 切换即时保存，下次启动自动恢复。
- **答题页内联退出对话框**：`Esc` 直接在答题页弹出「保存断点并退出 / 不保存退出 / 继续答题」，不再调用宿主嵌套弹窗。

### 修复

- 修复退出练习时调用宿主 `ui.select` 造成的**界面卡死**（busy 状态无法解除、按键全部失效）。
- 修复高亮在部分终端主题下**看不见**的问题：标记色改用固定 ANSI 序列，不再依赖宿主 `theme.fg` 的颜色解析。
- 修复超窄终端（宽度 1）下含高亮的宽字符**行越界**问题。

### 健壮性

- API 请求对**网络异常与 5xx** 做指数退避自动重试（最多 3 次），4xx 确定性错误不重试。
- 非 JSON 响应（网关错误页等）给出可读错误，不再抛出裸解析异常。
- 首次使用/Token 失效时的输入提示补全获取步骤，降低上手门槛。

### 工程化

- 新增 CI 工作流：`npm run build` + `npm test`（Node 20）。
- 新增 MIT `LICENSE`、`CHANGELOG.md`，补全 `package.json` 的仓库/主页/发布文件字段。

## [1.0.0]

- 首个可用版本：章节练习、每日一练、错题本、模拟考试、答题闯关五大模式，本地断点续做，AI 点拨与反问闪卡伴学。
